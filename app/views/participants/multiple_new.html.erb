<% content_for :head do %>
  <meta name="turbo-visit-control" content="reload">
<% end %>

<div class="container mx-auto p-6">
  <h1 class="text-4xl font-bold text-gray-800 mb-4">
    <%=t("participants.new.heading", tournament: @tournament.name) %>
  </h1>

  TODO
  <%= elm_embed('Elm.Participants', { flags: @flags }) %>

  <pre id="elm"></pre>
</div>
