<div class="container mx-auto p-6">
  <h1 class="text-4xl font-bold text-gray-800 mb-4">
    <%=t(".heading", tournament: @tournament.name) %>
  </h1>

  <%= elm_embed('Elm.Participants', { flags: @flags }) %>

  <pre id="elm"></pre>
</div>
