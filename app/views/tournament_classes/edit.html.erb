<% content_for :title, "Editing tournament class" %>

<div class="w-full p-6">
  <h1 class="font-bold text-4xl">Editing tournament class</h1>

  <%= render "form", tournament: @tournament, tournament_class: @tournament_class %>

  <%= link_to "Show this tournament class", tournament_tournament_class_path(@tournament_class), class: "ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
  <%= link_to "Back to tournament classes", tournament_tournament_classes_path, class: "ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
