<% content_for :title, "New tournament class" %>

<div class="w-full p-6">
  <h1 class="font-bold text-4xl">New tournament class</h1>

  <%= render "form", tournament: @tournament, group: @group %>

  <%= link_to "Back to tournament classes", tournament_groups_path(@tournament), class: "ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
